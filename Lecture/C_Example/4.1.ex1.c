#include<stdio.h>

int main()
{
	int a;
	printf("0~100점 점수를 입력하시오: ");
	scanf_s("%d", &a);

	if
	{ if ((a>=0)&&(a<=100))
		{ 
		if (a >= 90 && a<=100)
		{
			printf("A");

		}
			else if (a >= 80)
		{

			printf("B");

		}
	else if (a >= 70)
	{
		printf("C");
	}

	else if (a >= 60)
	{
		printf("D");
	}

	else
	{

		printf("F");

	}
	}
	}
}

